
# ⚡ Queue Pulse Tracker

A lightweight Streamlit app to log the “mood” of a support queue and visualize trends.

## Features
- Log a mood (emoji) + optional note
- Saves to Google Sheets
- Shows bar chart + average mood score

## How to Run Locally
1. Install dependencies:
   ```bash
   pip install -r requirements.txt
